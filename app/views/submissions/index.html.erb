# <p id="notice"><%= notice %></p>

# <h1>Submissions</h1>

# <table>
#   <thead>
#     <tr>
#       <th>Grade</th>
#       <th>Submission zip</th>
#       <th colspan="3"></th>
#     </tr>
#   </thead>

#   <tbody>
#     <% @submissions.each do |submission| %>
#       <tr>
#         <td><%= submission.grade %></td>
#         <td><%= submission.submission_zip %></td>
#         <td><%= link_to 'Show', submission %></td>
#         <td><%= link_to 'Edit', edit_submission_path(submission) %></td>
#         <td><%= link_to 'Destroy', submission, method: :delete, data: { confirm: 'Are you sure?' } %></td>
#       </tr>
#     <% end %>
#   </tbody>
# </table>

# <br>

# <%= link_to 'New Submission', new_submission_path %>

<% if !flash[:notice].blank? %>
   <div class = "alert alert-info">
      <%= flash[:notice] %>
   </div>
<% end %>

<br />

<%= link_to "New Submission", new_submission_path, class: "btn btn-primary" %>
<br />
<br />

<table class = "table table-bordered table-striped">
   <thead>.
      <tr>
         <th>Name</th>
         <th>Download Link</th>
         <th> </th>
      </tr>
   </thead>
   
   <tbody>
      <% @submissions.each do |submission| %>
         
         <tr>
            <td><%= submission.name %></td>
            <td><%= link_to "Download Submission", submission.attachment_url %></td>
            <td><%= button_to "Delete",  submission, method: :delete, class: "btn btn-danger", confirm: "Are you sure that you wish to delete #{submission.name}?" %></td>
         </tr>
         
      <% end %>
   </tbody>
   
</table>